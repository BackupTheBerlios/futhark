(##namespace ("utils-compile#"))
(##include "~~/lib/gambit#.scm")

(declare
  (standard-bindings)
  (extended-bindings)
  (block)
  ;;(not safe)
  (fixnum)
  )
          

(define *-loaded-*
  (make-table))

(define (load-directory d #!optional (recur? #t))
  (
  