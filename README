To test this:

Download and install Gambit-C scheme System.
at <http://dynamo.iro.umontreal.ca/~gambit/wiki/index.php/Main_Page>

to install run gsi make/install (you have to have write access to main Gambit-C directory)

run tests in test directory see test/README (futhark should be installed)

to be able to load openssl wrapper you have to install openssl and opessl headers,
then compile the file ~~/site-scheme/futhark/encode/openssl.scm

> cd /usr/local/Gambit-C/current/site-scheme/futhark/encode
> gsc -cc-options "-lssl -lcrypt" openssl

to be able to load ehwas/w3c-logger you have to compile it

> cd /usr/local/Gambit-C/current/site-scheme/futhark/ehwas
> gsc w3c-logger
