files "prelude.yera";
-- files "gui.yera";

Test = 
    struct : (main) ->
    open YeraCore, YeraDom, YeraUtils, YeraMath, XmlnsXhtml, MouseEvents;
    open YeraUserevent;

    files "mouse_events@.yera";

    x = floor (local / 1000);
    y = 20;
    z = x + y;
    sum = x + "+" + y + "=" + z;
 
    e1 = `<img src = "MisterWong.png"/>;
    e2 = `<img src = "Reddit.png"/>;
    
    o = {a: 1, b: 2};

    elem = e1 until (lbc out e1) and const e2;

    main = elem;
           
    -- main = `<div>@userevent</div>;
end;